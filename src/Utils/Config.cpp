#include "../include/Utils/Config.h"
#include <iostream>

void Config::load(const char* filepath) {
    std::cout << "Loading config from " << filepath << "\n";
    // Implementation of config loading
}

